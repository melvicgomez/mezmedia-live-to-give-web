/*
 * ClubDetailsPage Messages
 *
 * This contains all the text for the ClubDetailsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ClubDetailsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ClubDetailsPage container!',
  },
});
