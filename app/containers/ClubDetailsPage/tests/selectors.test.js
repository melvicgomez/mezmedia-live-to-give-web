// import { selectClubDetailsPageDomain } from '../selectors';

describe('selectClubDetailsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
