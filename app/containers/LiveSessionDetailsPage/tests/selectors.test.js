// import { selectLiveSessionDetailsPageDomain } from '../selectors';

describe('selectLiveSessionDetailsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
