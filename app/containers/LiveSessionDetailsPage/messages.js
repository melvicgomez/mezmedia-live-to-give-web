/*
 * LiveSessionDetailsPage Messages
 *
 * This contains all the text for the LiveSessionDetailsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LiveSessionDetailsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LiveSessionDetailsPage container!',
  },
});
