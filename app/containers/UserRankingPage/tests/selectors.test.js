// import { selectUserRankingPageDomain } from '../selectors';

describe('selectUserRankingPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
