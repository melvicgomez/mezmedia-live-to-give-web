// import { selectMyInterestPageDomain } from '../selectors';

describe('selectMyInterestPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
