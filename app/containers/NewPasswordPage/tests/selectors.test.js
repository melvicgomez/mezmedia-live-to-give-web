// import { selectNewPasswordPageDomain } from '../selectors';

describe('selectNewPasswordPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
