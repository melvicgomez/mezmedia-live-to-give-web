// import { selectLiveSessionIndexPageDomain } from '../selectors';

describe('selectLiveSessionIndexPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
