/*
 * LiveSessionIndexPage Messages
 *
 * This contains all the text for the LiveSessionIndexPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LiveSessionIndexPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LiveSessionIndexPage container!',
  },
});
