// import { selectOneTimePinPageDomain } from '../selectors';

describe('selectOneTimePinPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
