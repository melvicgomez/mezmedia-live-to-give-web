// import { selectAboutAppPageDomain } from '../selectors';

describe('selectAboutAppPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
