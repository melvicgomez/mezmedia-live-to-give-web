/*
 * DashboardAnalyticsContainer Messages
 *
 * This contains all the text for the DashboardAnalyticsContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DashboardAnalyticsContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DashboardAnalyticsContainer container!',
  },
});
