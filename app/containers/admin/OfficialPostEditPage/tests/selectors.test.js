// import { selectOfficialPostEditPageDomain } from '../selectors';

describe('selectOfficialPostEditPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
