// import { selectMeetupListPageDomain } from '../selectors';

describe('selectMeetupListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
