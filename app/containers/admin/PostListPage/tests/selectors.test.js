// import { selectPostListPageDomain } from '../selectors';

describe('selectPostListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
