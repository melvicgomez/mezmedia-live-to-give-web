/*
 * SendNotificationsContainer Messages
 *
 * This contains all the text for the SendNotificationsContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SendNotificationsContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SendNotificationsContainer container!',
  },
});
