// import { selectMeetupCreatePageDomain } from '../selectors';

describe('selectMeetupCreatePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
