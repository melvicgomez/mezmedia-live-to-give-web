// import { selectPollDetailsPageDomain } from '../selectors';

describe('selectPollDetailsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
