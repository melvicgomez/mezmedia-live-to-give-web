// import { selectMeetupEditPageDomain } from '../selectors';

describe('selectMeetupEditPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
