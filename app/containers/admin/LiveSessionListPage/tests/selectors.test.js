// import { selectLiveSessionListPageDomain } from '../selectors';

describe('selectLiveSessionListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
