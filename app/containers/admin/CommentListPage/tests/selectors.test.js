// import { selectCommentListPageDomain } from '../selectors';

describe('selectCommentListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
