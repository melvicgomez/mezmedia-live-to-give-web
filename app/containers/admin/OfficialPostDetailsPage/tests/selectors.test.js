// import { selectOfficialPostDetailsPageDomain } from '../selectors';

describe('selectOfficialPostDetailsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
