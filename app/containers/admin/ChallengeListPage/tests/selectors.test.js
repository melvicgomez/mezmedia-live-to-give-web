// import { selectCHallengeListPageDomain } from '../selectors';

describe('selectCHallengeListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
