// import { selectTeamDetailsPageDomain } from '../selectors';

describe('selectTeamDetailsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
