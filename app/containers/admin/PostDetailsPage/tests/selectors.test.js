// import { selectPostDetailsPageDomain } from '../selectors';

describe('selectPostDetailsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
