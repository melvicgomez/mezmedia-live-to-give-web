// import { selectCommentDetailsPageDomain } from '../selectors';

describe('selectCommentDetailsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
