// import { selectPollCreatePageDomain } from '../selectors';

describe('selectPollCreatePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
