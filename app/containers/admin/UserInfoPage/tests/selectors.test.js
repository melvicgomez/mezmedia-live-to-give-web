// import { selectUserInfoPageDomain } from '../selectors';

describe('selectUserInfoPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
