// import { selectLiveSessionCreatePageDomain } from '../selectors';

describe('selectLiveSessionCreatePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
