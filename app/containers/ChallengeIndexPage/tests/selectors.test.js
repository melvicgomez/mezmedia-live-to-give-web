// import { selectChallengeIndexPageDomain } from '../selectors';

describe('selectChallengeIndexPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
