/*
 * ChallengeIndexPage Messages
 *
 * This contains all the text for the ChallengeIndexPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ChallengeIndexPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ChallengeIndexPage container!',
  },
});
