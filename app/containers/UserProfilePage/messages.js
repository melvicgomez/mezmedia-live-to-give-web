/*
 * UserProfilePage Messages
 *
 * This contains all the text for the UserProfilePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UserProfilePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserProfilePage container!',
  },
});
