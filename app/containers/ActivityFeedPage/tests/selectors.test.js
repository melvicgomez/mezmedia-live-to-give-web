// import { selectActivityFeedPageDomain } from '../selectors';

describe('selectActivityFeedPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
