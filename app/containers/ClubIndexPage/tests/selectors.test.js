// import { selectClubIndexPageDomain } from '../selectors';

describe('selectClubIndexPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
