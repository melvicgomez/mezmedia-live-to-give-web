/*
 * ClubIndexPage Messages
 *
 * This contains all the text for the ClubIndexPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ClubIndexPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ClubIndexPage container!',
  },
});
