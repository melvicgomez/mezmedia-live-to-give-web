// import { selectCharityIndexPageDomain } from '../selectors';

describe('selectCharityIndexPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
