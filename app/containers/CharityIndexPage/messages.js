/*
 * CharityIndexPage Messages
 *
 * This contains all the text for the CharityIndexPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CharityIndexPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CharityIndexPage container!',
  },
});
