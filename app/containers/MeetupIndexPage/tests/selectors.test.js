// import { selectMeetupIndexPageDomain } from '../selectors';

describe('selectMeetupIndexPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
