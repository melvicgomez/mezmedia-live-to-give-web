// import { selectMeetupDetailsPageDomain } from '../selectors';

describe('selectMeetupDetailsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
