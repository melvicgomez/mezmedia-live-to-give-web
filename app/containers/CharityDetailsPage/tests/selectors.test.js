// import { selectCharityDetailsPageDomain } from '../selectors';

describe('selectCharityDetailsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
