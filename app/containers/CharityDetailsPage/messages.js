/*
 * CharityDetailsPage Messages
 *
 * This contains all the text for the CharityDetailsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CharityDetailsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CharityDetailsPage container!',
  },
});
