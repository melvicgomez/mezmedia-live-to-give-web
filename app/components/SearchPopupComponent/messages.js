/*
 * SearchPopupComponent Messages
 *
 * This contains all the text for the SearchPopupComponent component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SearchPopupComponent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SearchPopupComponent component!',
  },
});
