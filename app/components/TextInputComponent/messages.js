/*
 * TextInputComponent Messages
 *
 * This contains all the text for the TextInputComponent component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TextInputComponent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TextInputComponent component!',
  },
});
